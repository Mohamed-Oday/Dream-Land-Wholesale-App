---
phase: 09-security-atomicity
plan: 03
type: tdd
wave: 1
depends_on: ["09-02"]
files_modified:
  - lib/features/orders/models/line_item.dart
  - lib/core/utils/order_calculator.dart
  - lib/features/orders/screens/create_order_screen.dart
  - test/unit/core/utils/version_utils_test.dart
  - test/unit/core/utils/order_calculator_test.dart
  - test/unit/features/auth/models/app_user_test.dart
  - test/unit/features/orders/models/line_item_test.dart
autonomous: true
---

<objective>
## Goal
Extract testable business logic from UI widgets and write a minimum unit test suite covering financial calculations, version comparison, and user model — the first automated tests in the project.

## Purpose
The codebase has zero automated tests. Critical financial calculation logic (`_LineItem`, order totals) is trapped inside private widget state, making it untestable. This plan extracts pure logic into testable modules and writes ~35 unit tests covering the core business rules that protect revenue accuracy.

## Output
- Extracted `LineItem` model (public, testable)
- Extracted `OrderCalculator` utility (pure functions)
- 4 test files with ~35 unit tests
- `flutter test` passes with all tests green
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/ROADMAP.md
@.paul/STATE.md

## Prior Work
@.paul/phases/09-security-atomicity/09-02-SUMMARY.md
- Plan 09-02 created `create_order_atomic` RPC and `isNewerVersion` utility — both need test coverage
- `isNewerVersion` was extracted to `lib/core/utils/version_utils.dart` — already testable

## Source Files — Code to Extract
@lib/features/orders/screens/create_order_screen.dart (lines 880-907: _LineItem class, lines 30-43: calculation getters)

## Source Files — Already Testable
@lib/core/utils/version_utils.dart (isNewerVersion — pure function)
@lib/features/auth/models/app_user.dart (AppUser model — pure model)

## Test Infrastructure
@test/widget_test.dart (placeholder only — no real tests exist)
@pubspec.yaml (flutter_test available, no mockito/mocktail needed for pure unit tests)
</context>

<skills>
## Required Skills (from SPECIAL-FLOWS.md)

No specialized skills required — this plan is code extraction + unit test writing. No UI work.
</skills>

<acceptance_criteria>

## AC-1: LineItem Extracted as Public Class
```gherkin
Given the _LineItem class is private inside create_order_screen.dart
When the extraction is complete
Then a public LineItem class exists at lib/features/orders/models/line_item.dart
And create_order_screen.dart imports and uses the extracted class
And flutter analyze passes with zero errors
```

## AC-2: Order Calculator Extracted as Pure Functions
```gherkin
Given order total calculations are embedded in _CreateOrderScreenState getters
When the extraction is complete
Then pure functions exist in lib/core/utils/order_calculator.dart:
  - calculateSubtotal(List<LineItem> items)
  - calculateTax(double subtotal, double taxPercentage)
  - calculateTotal(double subtotal, double taxAmount, double discount)
And create_order_screen.dart uses the extracted functions
```

## AC-3: All Unit Tests Pass
```gherkin
Given the test suite is written
When running flutter test
Then all ~35 tests pass
And test output shows 0 failures
```

## AC-4: Existing Functionality Unchanged
```gherkin
Given the code extractions modify create_order_screen.dart
When flutter analyze runs on the full project
Then zero errors are reported in lib/
And the order creation flow behaves identically (same calculations, same RPC call)
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Extract LineItem and OrderCalculator from Widget State</name>
  <files>lib/features/orders/models/line_item.dart, lib/core/utils/order_calculator.dart, lib/features/orders/screens/create_order_screen.dart</files>
  <action>
    **Extract LineItem (lib/features/orders/models/line_item.dart):**
    - Copy `_LineItem` class from create_order_screen.dart (lines 880-907)
    - Make it public: rename `_LineItem` → `LineItem`
    - Keep all fields and getters: `productId`, `productName`, `unitPrice`, `unitsPerPackage`, `hasReturnablePackaging`, `stockOnHand`, `quantity`, `packagePrice`, `lineTotal`, `totalPieces`
    - No other changes — identical logic, just public

    **Extract OrderCalculator (lib/core/utils/order_calculator.dart):**
    - Create pure top-level functions (not a class):
      - `double calculateSubtotal(List<LineItem> items)` — fold sum of lineTotal
      - `double calculateTax(double subtotal, double taxPercentage)` — subtotal * percentage / 100
      - `double calculateTotal(double subtotal, double taxAmount, double discount)` — subtotal + taxAmount - discount
      - `double parseDiscount(String text)` — trim, tryParse, return 0 on empty/invalid
    - Import LineItem from the new model file

    **Update create_order_screen.dart:**
    - Remove `_LineItem` class (lines 880-907)
    - Add import: `import '../../orders/models/line_item.dart';` (or correct relative path)
    - Add import for order_calculator
    - Replace `_LineItem` references with `LineItem`
    - Replace calculation getters with calls to extracted functions:
      - `_subtotal` → `calculateSubtotal(_lineItems)`
      - `_taxAmount` → `calculateTax(_subtotal, _taxPercentage)`
      - `_total` → `calculateTotal(_subtotal, _taxAmount, _discountAmount)`
      - `_discountAmount` → `parseDiscount(_discountController.text)`
    - Keep `_taxPercentage => 0` as a local getter (it's a UI constant, not business logic)
    - Keep `_hasDiscount` and `_canSubmit` as local getters (they depend on UI state)

    Avoid:
    - Do NOT change any logic — just move it to a testable location
    - Do NOT add new functionality
    - Do NOT change the RPC call or receipt navigation
  </action>
  <verify>
    1. `cd apps/dream-land-shopping && flutter analyze --no-pub lib/` — zero errors
    2. Verify LineItem class is public in its own file
    3. Verify OrderCalculator has 4 pure functions
    4. Verify _LineItem no longer exists in create_order_screen.dart
  </verify>
  <done>AC-1 (LineItem extracted) and AC-2 (OrderCalculator extracted) and AC-4 (analyze passes) satisfied</done>
</task>

<task type="auto">
  <name>Task 2: Write Unit Tests for All Pure Business Logic</name>
  <files>test/unit/core/utils/version_utils_test.dart, test/unit/core/utils/order_calculator_test.dart, test/unit/features/auth/models/app_user_test.dart, test/unit/features/orders/models/line_item_test.dart</files>
  <action>
    **Create test directory structure:**
    - `test/unit/core/utils/`
    - `test/unit/features/auth/models/`
    - `test/unit/features/orders/models/`

    **version_utils_test.dart (~10 tests):**
    - `isNewerVersion('2.0.0', '1.0.0')` → true (major bump)
    - `isNewerVersion('1.1.0', '1.0.0')` → true (minor bump)
    - `isNewerVersion('1.0.1', '1.0.0')` → true (patch bump)
    - `isNewerVersion('1.0.0', '1.0.0')` → false (equal)
    - `isNewerVersion('1.0.0', '2.0.0')` → false (older)
    - `isNewerVersion('0.2.0', '0.2.0')` → false (current app version)
    - `isNewerVersion('0.2.1', '0.2.0')` → true (next patch)
    - `isNewerVersion('10.0.0', '9.9.9')` → true (multi-digit)
    - `isNewerVersion('1.0', '1.0.0')` → false (shorter equal)
    - `isNewerVersion('1.0.0.1', '1.0.0')` → true (longer version)

    **line_item_test.dart (~8 tests):**
    - packagePrice with unitsPerPackage (e.g., 50 DA × 10 units = 500 DA/package)
    - packagePrice without unitsPerPackage (unitPrice returned directly)
    - lineTotal = packagePrice × quantity
    - lineTotal with quantity > 1
    - totalPieces with unitsPerPackage (e.g., 3 packages × 10 = 30 pieces)
    - totalPieces without unitsPerPackage → null
    - quantity mutation updates lineTotal
    - zero quantity edge case

    **order_calculator_test.dart (~10 tests):**
    - calculateSubtotal with multiple items
    - calculateSubtotal with empty list → 0
    - calculateSubtotal with single item
    - calculateTax with 0% → 0
    - calculateTax with 19% (standard tax)
    - calculateTotal with all components
    - calculateTotal with zero discount
    - calculateTotal with discount equal to subtotal → 0
    - parseDiscount with valid number → parsed value
    - parseDiscount with empty string → 0
    - parseDiscount with non-numeric → 0

    **app_user_test.dart (~8 tests):**
    - Constructor with all fields
    - isOwner/isAdmin/isDriver return correct booleans
    - rolePath returns '/owner', '/admin', '/driver'
    - fromSupabaseUser with complete metadata
    - fromSupabaseUser with missing metadata → defaults (role='driver', active=true)
    - fromSupabaseUser with empty businessId

    **Test patterns:**
    - Use `group()` to organize related tests
    - Use descriptive test names in Arabic context (e.g., "package price for 10-unit bread package")
    - <!-- audit-added --> Use `closeTo(expected, 0.001)` matcher for all double assertions — floating-point arithmetic can produce precision errors (e.g., 0.1+0.2 ≠ 0.3), which would cause false test failures in financial calculations
    - All tests are pure unit tests — no mocking, no async, no Flutter widgets
    - <!-- audit-added --> For `AppUser.fromSupabaseUser` tests: construct `User` directly with `User(id: 'test-id', appMetadata: {}, userMetadata: {'role': 'owner', 'business_id': 'biz-1'}, aud: 'authenticated', createdAt: DateTime.now().toIso8601String())` — this is test data construction, not mocking

    <!-- audit-added --> **Delete broken placeholder test (MANDATORY):**
    - DELETE `test/widget_test.dart` — it references non-existent `MyApp` class and will cause `flutter test` to fail. This is not optional; AC-3 cannot pass otherwise.

    Avoid:
    - Do NOT add mockito or other test dependencies — pure flutter_test is sufficient
    - Do NOT write widget tests — this is unit tests only
    - Do NOT write integration tests against Supabase
  </action>
  <verify>
    1. <!-- audit-added --> Verify test/widget_test.dart is deleted
    2. `cd apps/dream-land-shopping && flutter test` — all tests pass, 0 failures
    3. Verify ~35 tests total across 4 files
    4. Verify no test uses mocking or async
  </verify>
  <done>AC-3 (all tests pass) satisfied</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- `supabase/migrations/*` — no migration changes in this plan
- `lib/features/orders/repositories/order_repository.dart` — repository is stable
- `lib/routing/app_router.dart` — router is stable
- `lib/features/auth/screens/*` — auth screens are stable
- Any calculation logic — extract, don't modify

## SCOPE LIMITS
- Unit tests only — no widget tests, no integration tests, no SQL tests
- No new dependencies (mockito, mocktail) — flutter_test is sufficient for pure logic
- No test coverage for Supabase RPCs (would need live instance or complex mocking)
- No test coverage for UI widgets or providers

</boundaries>

<verification>
Before declaring plan complete:
- [ ] `cd apps/dream-land-shopping && flutter analyze --no-pub lib/` — zero errors
- [ ] `flutter test` — all tests pass, 0 failures
- [ ] LineItem class exists as public in lib/features/orders/models/line_item.dart
- [ ] OrderCalculator functions exist in lib/core/utils/order_calculator.dart
- [ ] create_order_screen.dart uses extracted code (no _LineItem, imports new files)
- [ ] ~35 tests across 4 test files
- [ ] All acceptance criteria met
</verification>

<success_criteria>
- Pure business logic extracted from widget state into testable modules
- ~35 unit tests covering financial calculations, version comparison, and user model
- `flutter test` passes with all green
- `flutter analyze` passes with zero errors
- Existing app functionality unchanged
</success_criteria>

<output>
After completion, create `.paul/phases/09-security-atomicity/09-03-SUMMARY.md`
</output>
